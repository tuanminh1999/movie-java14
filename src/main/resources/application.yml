spring:

  datasource:
    url: jdbc:postgresql://localhost:5432/movie
    username: postgres
    password: admin
    
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true
    driver-class-name: org.postgresql.Driver